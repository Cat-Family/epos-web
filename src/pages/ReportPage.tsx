import React from "react";
import { Box, Typography } from "@mui/material";

function ReportPage() {
  return (
    <Box
      sx={{
        flexGrow: 1,
        bgcolor: "background.paper",
        display: "flex",
        width: 1,
        p: 1,
      }}
    >
      <Typography>ReportPage</Typography>
    </Box>
  );
}

export default ReportPage;
